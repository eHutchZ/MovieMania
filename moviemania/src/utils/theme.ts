import { createTheme } from '@mui/material';

const theme = createTheme({
  typography: {
    h2: {
      fontSize: '32px',
    },
    caption: {
      fontSize: '14px',
    },
  },
});

export default theme;
